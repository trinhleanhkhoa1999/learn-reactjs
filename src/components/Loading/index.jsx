import React from 'react';
import './styles.scss';

Loading.propTypes = {

};

function Loading(props) {
    return (
        <div>
            Loading..........
        </div>
    );
}

export default Loading;